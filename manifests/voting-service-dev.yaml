apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voting-service-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/VotingM7011E/gitops.git
    targetRevision: main
    path: voting-service
    helm:
      valueFiles:
        - ../environments/dev/voting-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: voting-service-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true